import 'package:flutter/material.dart';

// Primary and secondary colors for the app
const primaryColor = Color(0xFF40BFFF);
const secondaryColor = Color(0xFFFB7181);
const yellowColor = Color(0xFFFFC833);
const greenColor = Color(0xFF53D1B6);
const purpleColor = Color(0xFF5C61F4);

// Neutral colors for the app
const darkColor = Color(0xFF223263);
const greyColor = Color(0xFF9098B1);
const lightColor = Color(0xFFEBF0FF);

// Background color for the app
const backgroundColor = Color(0xFFFFFFFF);
const Color primaryLight = Color(0xFF818CF8);
const Color primaryDark = Color(0xFF3730A3);
const Color tertiaryColor = Color(0xFF14B8A6);

const List<Color> primaryGradient = [
  primaryColor,
  Color(0xFF6366F1),
];

const Color surfaceColor = Color(0xFFFFFFFF);
const Color textPrimary = Color(0xFF1E293B);
const Color textSecondary = Color(0xFF64748B);

const Color success = Color(0xFF22C55E);
const Color error = Color(0xFFEF4444);
const Color warning = Color(0xFFF59E0B);
